#include <stdio.h>

/**
 * 一球从100米高度自由落下，每次落地后反跳回原高度的一半,再落下
 * 求它在第10次落地时，共经过多少米？第10次反弹多高？
 */
int main() {
    float m = 100, h = 100;
    h = h / 2;
    for (int i = 2; i <= 10; i++) {
        m += h * 2;
        h /= 2;
    }

    printf("共经过：%f米,第10次反弹：%f高\n", m, h);
}